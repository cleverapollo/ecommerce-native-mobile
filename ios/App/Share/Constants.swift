import Foundation
import UIKit

struct Constants {
    
    static let cornerRadius = 25.0
    static let defaultFontSize = 14.0
    static let defaultMargin = 8.0
    // spacing below table view
    static var tableViewInsets: UIEdgeInsets {
        UIEdgeInsets(top: 0, left: 0, bottom: 50, right: 0)
    }
    
    // Validation
    static let maxAllowedChars = 255
}
